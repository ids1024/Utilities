#!/bin/bash
if [[ "$1" == "-u" ]] #Update, should be done in a cron job
then
	curl http://cryptocrack.de/files/aurpkglist.txt.gz | zcat | cut -d' ' -f1 | tail -n +2 | sed s/%2B/+/g | sort | sponge ~/.lsaur
else
	cat ~/.lsaur
fi
