#!/bin/bash
if [[ "$1" == "-u" ]] #Update, should be done in a cron job
then
	curl http://cryptocrack.de/files/aurpkglist.txt.gz | zcat | awk 'NR>1{gsub("%2B","+");print $1}' | sort | sponge ~/.lsaur

else
	cat ~/.lsaur
fi
